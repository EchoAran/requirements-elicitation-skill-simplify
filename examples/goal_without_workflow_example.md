# goal without workflow example

## Input

### user_input
We need to increase enterprise customer retention this year.

## Expected handling

1. Treat this as product objective evidence, not as complete requirements.
2. Ask one compact scoping question to identify target user and main workflow.
3. Keep phase in `start` or early `runtime` until workflow information is grounded.

## Good next utterance pattern

```text
The goal is clear. To translate it into actionable requirements, I need one key clarification first: which user segment should improve retention through which core workflow?
```
